export interface SomeTableCreateUpdateDTO {
    id?: number | string;
    email: string;
    normalText: string;
    phoneNumber: string;
    startDate: string;
    endDate: string;
    status: boolean;
    type: string;
}